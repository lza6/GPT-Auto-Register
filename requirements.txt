# GPT 自动注册 — 依赖清单（唯一依赖来源，启动.bat 据此安装）
# 核心运行
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
loguru>=0.7
psutil>=5.9
requests>=2.31
curl_cffi>=0.7

# 邮箱取码（IMAP 直连 Outlook）
imapclient>=3.0

# 浏览器注册 / CF Solver（camoufox 首次运行会下载浏览器数据）
camoufox[fetch]>=0.4

# 测试
pytest>=8.0
pytest-asyncio>=0.23
coverage>=7.4
